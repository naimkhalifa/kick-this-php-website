<?php

$configPath = dirname(realpath(__FILE__)) . "/";

// Define some useful constants
define('BASE_PATH', dirname($configPath));
define('PUBLIC_PATH', BASE_PATH . '/www/');
define('APP_PATH', BASE_PATH . '/app/');
